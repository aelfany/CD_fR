#include<stdio.h>
#include<string.h>
#include<stdlib.h>

int main()
{
	int x;
	int y;
	int b = 0;
	char *str;
	scanf("%d", &x);
	while(x >= 1)
	{
		scanf("%[i^\n]%*c", str);
		printf("%s", str);
		int y = strlen(str);
		while(b < y)
		{
			if(x == 1)
			{
				while()
			}
		}
		x--;
	}
}
